# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Tap Master' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Tap Master
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'

  target 'Tap MasterTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Tap MasterUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
